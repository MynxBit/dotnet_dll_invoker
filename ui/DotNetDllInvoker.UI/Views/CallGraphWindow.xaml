<Window x:Class="DotNetDllInvoker.UI.Views.CallGraphWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Call Graph Visualization" Height="800" Width="1200"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WindowBackground}">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="400"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="350" MinWidth="200"/>
        </Grid.ColumnDefinitions>
        
        <!-- Graph Canvas -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Toolbar -->
            <Border Grid.Row="0" Background="{StaticResource SurfaceBackground}" Padding="10" BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderBrush}">
                <DockPanel>
                    <TextBlock Text="📊 Call Graph" FontWeight="Bold" FontSize="16" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <TextBlock Text="{Binding Edges.Count, StringFormat='Edges: {0}'}" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" Margin="10,0"/>
                        <Button Content="🔄 Refresh Layout" Command="{Binding RefreshCommand}" Style="{StaticResource ModernButtonStyle}"/>
                    </StackPanel>
                </DockPanel>
            </Border>
            
            <!-- Canvas with Edges and Nodes -->
            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="#111111">
                <Grid Width="3000" Height="2000">
                    <!-- Edges Layer (drawn first, behind nodes) -->
                    <ItemsControl ItemsSource="{Binding Edges}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas IsItemsHost="True"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Line X1="{Binding X1}" Y1="{Binding Y1}" 
                                      X2="{Binding X2}" Y2="{Binding Y2}">
                                    <Line.Style>
                                        <Style TargetType="Line">
                                            <Setter Property="Stroke" Value="#444444"/>
                                            <Setter Property="StrokeThickness" Value="1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                                                    <Setter Property="Stroke" Value="#FFD700"/>
                                                    <Setter Property="StrokeThickness" Value="3"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Line.Style>
                                </Line>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    
                    <!-- Nodes Layer -->
                    <ItemsControl ItemsSource="{Binding Nodes}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas IsItemsHost="True"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding X}"/>
                                <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Border Width="160" Height="50" CornerRadius="5" 
                                            BorderThickness="2" Cursor="Hand"
                                            MouseLeftButtonDown="OnNodeClick"
                                            ToolTip="{Binding Id}">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#1E1E1E"/>
                                                <Setter Property="BorderBrush" Value="#3C3C3C"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasOutgoingEdges}" Value="True">
                                                        <Setter Property="BorderBrush" Value="#4EC9B0"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsExternal}" Value="True">
                                                        <Setter Property="Background" Value="#2D2D30"/>
                                                        <Setter Property="BorderBrush" Value="#555555"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                                        <Setter Property="Background" Value="#264F78"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <TextBlock Text="{Binding DisplayName}" 
                                                       Foreground="{StaticResource TextPrimary}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Left"
                                                       TextTrimming="CharacterEllipsis"
                                                       TextWrapping="Wrap"
                                                       FontSize="11"
                                                       Margin="8,5,35,5"/>
                                            <!-- Invoke Button -->
                                            <Button Content="▶" 
                                                    HorizontalAlignment="Right" 
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,5,0"
                                                    Width="24" Height="24"
                                                    FontSize="12"
                                                    Background="#E74C3C"
                                                    Foreground="White"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    ToolTip="Invoke this method"
                                                    Click="OnNodeInvokeClick"
                                                    Tag="{Binding}">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </ScrollViewer>
            
            <!-- Status -->
            <StatusBar Grid.Row="2" Background="{StaticResource SurfaceBackground}">
                <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource TextMuted}"/>
            </StatusBar>
        </Grid>
        
        <!-- GridSplitter -->
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="{StaticResource BorderBrush}" Cursor="SizeWE"/>
        
        <!-- Side Panel: Code View -->
        <Grid Grid.Column="2" Background="{StaticResource PanelBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Border Grid.Row="0" Padding="10" BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderBrush}">
                <TextBlock Text="Selected Method" FontWeight="Bold" Foreground="{StaticResource TextSecondary}"/>
            </Border>
            
            <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="5">
                <TabItem Header="IL">
                    <TextBox Text="{Binding SelectedNodeIL, Mode=OneWay}" 
                             IsReadOnly="True" 
                             FontFamily="Consolas" 
                             FontSize="11"
                             Background="#111111" 
                             Foreground="#CE9178" 
                             BorderThickness="0"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             TextWrapping="NoWrap"/>
                </TabItem>
                <TabItem Header="C#">
                    <TextBox Text="{Binding SelectedNodeCode, Mode=OneWay}" 
                             IsReadOnly="True" 
                             FontFamily="Consolas" 
                             FontSize="11"
                             Background="#111111" 
                             Foreground="#569CD6" 
                             BorderThickness="0"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             TextWrapping="NoWrap"/>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>

