<UserControl x:Class="DotNetDllInvoker.UI.Views.ObjectWorkbenchPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="300">
    
    <Grid Background="{StaticResource SurfaceBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="10">
            <DockPanel>
                <TextBlock Text="🔬 Object Workbench" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="🔄" Command="{Binding RefreshCommand}" ToolTip="Refresh" Width="28" Height="28" Margin="2,0"/>
                    <Button Content="🗑" Command="{Binding ClearAllCommand}" ToolTip="Clear All" Width="28" Height="28"/>
                </StackPanel>
            </DockPanel>
        </Border>
        
        <!-- Instance List -->
        <ListView Grid.Row="1" ItemsSource="{Binding Instances}" SelectedItem="{Binding SelectedInstance}" 
                  BorderThickness="0" Background="Transparent" Margin="5">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="3">
                        <DockPanel>
                            <TextBlock Text="{Binding Id}" Foreground="{StaticResource TextMuted}" FontSize="10" DockPanel.Dock="Left"/>
                            <Button Content="×" Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                    CommandParameter="{Binding}" DockPanel.Dock="Right" 
                                    Width="16" Height="16" FontSize="10" Background="Transparent" BorderThickness="0"
                                    Foreground="{StaticResource DangerBrush}" ToolTip="Remove"/>
                        </DockPanel>
                        <TextBlock Text="{Binding Label}" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="{Binding TypeName}" FontSize="10" Foreground="{StaticResource TextMuted}" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <!-- Properties Header -->
        <Border Grid.Row="2" Background="{StaticResource PanelBackground}" Padding="10,5">
            <TextBlock Text="Properties" FontWeight="Bold" Foreground="{StaticResource TextSecondary}"/>
        </Border>
        
        <!-- Properties Grid -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding SelectedInstance.Properties}" AutoGenerateColumns="False"
                  Background="Transparent" BorderThickness="0" IsReadOnly="True" HeadersVisibility="Column"
                  CanUserAddRows="False" CanUserDeleteRows="False" GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{StaticResource BorderBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="80" Foreground="{StaticResource TextPrimary}"/>
                <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" Width="60" Foreground="{StaticResource TextMuted}"/>
                <DataGridTextColumn Header="Value" Binding="{Binding ValueString}" Width="*" Foreground="#DCDCAA"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Empty State -->
        <TextBlock Grid.Row="1" Grid.RowSpan="3" 
                   Text="No instances. Use 'Keep Alive' when invoking methods." 
                   Foreground="{StaticResource TextMuted}" 
                   VerticalAlignment="Center" HorizontalAlignment="Center" 
                   FontStyle="Italic" TextWrapping="Wrap" Margin="20">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Instances.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
